spring:
  application:
    name: ui
    
eureka:  instance:    non-secure-port: ${server.port:80}
  client:    eureka-server-url:
      defaultZone: http://${eureka.host:localhost}:${eureka.port:8761}/eureka/